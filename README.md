
  # School Student Attention Tracker

  This is a code bundle for School Student Attention Tracker. The original project is available at https://www.figma.com/design/EFRrhlWJDPXg3882tnmunD/School-Student-Attention-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  